const amount=document.querySelector('#amount'),meal=document.querySelector('#meal-count');function updateImpact(){let n=+amount.value||0;meal.textContent=`${Math.floor(n/50).toLocaleString('en-IN')} meal${Math.floor(n/50)===1?'':'s'}`}new URLSearchParams(location.search).get('amount')&&(amount.value=new URLSearchParams(location.search).get('amount'));updateImpact();amount?.addEventListener('input',updateImpact);document.querySelectorAll('[data-amount]').forEach(b=>b.onclick=()=>{amount.value=b.dataset.amount;updateImpact();document.querySelectorAll('[data-amount]').forEach(x=>x.classList.remove('selected'));b.classList.add('selected')});document.querySelectorAll('[data-copy]').forEach(b=>b.onclick=async()=>{await navigator.clipboard.writeText(b.dataset.copy);let t=b.textContent;b.textContent='Copied!';setTimeout(()=>b.textContent=t,1400)});
