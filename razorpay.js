// Production: fetch a server-created order from POST /api/donation/create-order, then open Razorpay Checkout with the returned order ID. Never place a Razorpay secret key in frontend code.
document.querySelector('#donate-form')?.addEventListener('submit',e=>{e.preventDefault();const form=new FormData(e.currentTarget);const donationAmount=Number(form.get('amount'));if(!donationAmount||donationAmount<1)return;/* Replace the demo below with: fetch('/api/donation/create-order',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({amount:donationAmount*100,...Object.fromEntries(form)})}) */document.querySelector('.payment-modal').classList.add('open')});document.querySelector('.close-modal')?.addEventListener('click',()=>document.querySelector('.payment-modal').classList.remove('open'));
